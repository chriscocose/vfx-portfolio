import WorkGrid from "@/components/WorkGrid";

export const metadata = { title: "Work — Your Name" };

export default function WorkPage() {
  return <WorkGrid />; // WorkGrid is a Client Component; this is fine.
}

